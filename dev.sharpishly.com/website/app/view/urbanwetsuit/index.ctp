{{{header}}}

<header>
    <h1>{{{h1}}}</h1>
    <p>{{{h2}}}</p>
</header>

{{{work}}}

<!--form {{{form}}}>
    <fieldset>
        <div class="row">
            <label for="myFile">Upload CV:</label>
            <input type="file" name="myFile" id="myFile" accept=".pdf, .doc, .docx" />
            <button type="submit">Submit Application</button>
        </div>
    </fieldset>
</form-->

{{{footer}}}