{{{header}}}

    <div class="layout-item">

        <h1>{{{h1}}}</h1>

        <h2>{{{h2}}}</h2>

        <table class="table">
            {{{baskets}}}
        </table>

        <p>
            <a {{{checkout}}}>Checkout</a>
            <a {{{shop}}}>Back to shop</a>
        </p>

    </div>

{{{footer}}}