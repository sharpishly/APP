{{{header}}}

    <div class="layout-item">
        <h1>{{{h1}}}</h1>
        <h2>{{{h2}}}</h2>
        <table class="table">
            <form {{{form}}}>
                <tr>
                    <td colspan="3">
                        <button type="submit" class="submit-button" id="submit">Update</button>
                    </td>
                </tr>
                {{{precart}}}
                {{{hidden}}}
            </form>
        </table>
    </div>

{{{footer}}}