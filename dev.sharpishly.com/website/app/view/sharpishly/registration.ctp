{{{header}}}

<!-- flash -->
<div {{{flash}}}>
    <p>Email is already in use</p>
</div>
<!-- flash -->

<!-- registration -->
<div class="layout-item">
    <h1>{{{h1}}}</h1>
    <h2>{{{h2}}}</h2>
        <!-- form -->
        <form {{{form}}}>
        <fieldset>
            <table class="table">
                <tr>
                    <td colspan="2">
                        <button type="submit" class="submit-button" id="submit">Register</button>
                    </td>
                </tr>
                {{{fields}}}
            </table>                    		
        </fieldset>
    </form>
    <!-- form -->
</div>
<!-- registration -->
{{{footer}}}